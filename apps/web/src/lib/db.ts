/**
 * Convenience re-export so route handlers / server components can:
 *   import { db } from '@/lib/db'
 * instead of reaching into the workspace package.
 */
export { db } from '@hyperspeed/db/client'
